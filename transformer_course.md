# Transformer 系统化课程

> 从零基础到理解现代 LLM 的完整课程
> 共 6 课 + 数学基础专题，适合小白入门

---

## 课程总览

| 课次 | 主题 | 核心内容 | 难度 |
|------|------|---------|------|
| 第1课 | 文本表示 | Tokenization、Embedding、权重共享 | ★★ |
| 第2课 | 位置编码 | 无序性问题、sin/cos 公式 | ★★★ |
| 第3课 | 注意力机制 | QKV、√d_k、softmax（含数学推导） | ★★★★★ |
| 第4课 | 注意力变体 | 自注意力、交叉注意力、多头、Masked | ★★★★ |
| 第5课 | 完整 Block | 残差、LayerNorm、FFN、训练推理 | ★★★★ |
| 第6课 | 现代 LLM | Pre-Norm、RMSNorm、RoPE、SwiGLU、GQA | ★★★ |
| 第7课 | 数据与训练 | 三阶段训练、数据格式、训练循环 | ★★★ |
| 附录 | 数学基础 | 期望、方差、正态分布、高斯积分 | ★★★★★ |

---

# 第1课：文本表示

## 1.1 Tokenization（分词）

### 问题：计算机不认识文字
模型只能处理数字，所以要把文本切成一个个单元（token），再转成 ID。

### BPE 算法（Byte Pair Encoding）
1. 把文本拆成字符
2. 统计高频字符对，合并成新 token
3. 重复，直到达到预设词表大小

```
文本: "low low low lower lower"
↓ BPE 训练
词表: ["l", "o", "w", "e", "r", "lo", "low", "er", "low", "lower"]
ID:    0     1    2    3    4    5     6      7      6      8
```

### 核心结论
- 每个 token 对应一个唯一 ID
- 词表大小 V 通常 3万~15万
- 子词切分能兼顾"未登录词"和"词表大小"

---

## 1.2 Embedding（词嵌入）

### 问题：ID 是离散的，没有语义
ID=4502 和 ID=4503 只是编号相邻，没有语义关系。需要把 ID 变成连续向量。

### 解决方案：查表
构建一个矩阵 E，形状 `[V, d]`（V=词表大小，d=维度，如512）。
- 每一行 = 一个词的向量
- 查表 = 用 ID 当行号取对应行

```
词表:
  ID=0: "猫"  → E[0] = [0.9, 0.1, ...]   ← 第0行
  ID=1: "狗"  → E[1] = [0.8, 0.2, ...]   ← 第1行
  ID=2: "鱼"  → E[2] = [0.1, 0.9, ...]   ← 第2行

E 矩阵形状 [3, 2]:
  ┌─────┬─────┐
  │ 0.9 │ 0.1 │  ← "猫"
  ├─────┼─────┤
  │ 0.8 │ 0.2 │  ← "狗"
  ├─────┼─────┤
  │ 0.1 │ 0.9 │  ← "鱼"
  └─────┴─────┘
```

### 关键性质
- 语义相近的词 → 向量相近（ cosine 相似度高）
- E 是可学习参数，训练时不断更新

---

## 1.3 权重共享：Embedding 与输出层

### 反直觉的设计
原版 Transformer 和很多 LLM 让 **Embedding 矩阵 E 和最后的分类层共享权重**。

| 角色 | 方向 | 操作 |
|------|------|------|
| 输入端（Embedding） | 词 → 向量 | E[ID]（查表） |
| 输出端（分类层） | 向量 → 词分数 | h · E^T（矩阵乘法） |

### 矩阵乘法验证
- E 形状 `[V, d]`，E^T 形状 `[d, V]`
- h 形状 `[1, d]`
- h · E^T = `[1, d] × [d, V] = [1, V]` → V 个分数 ✓

### 为什么合理
- 编码（词→向量）和解码（向量→词概率）是互逆过程
- 用同一个矩阵的转置，逻辑自洽
- 省一半参数（V×d 的矩阵很大）

### 余弦相似度（为什么输出层用点积）
```
cos(a, b) = (a · b) / (|a| · |b|)
```
- 分子是点积，分母归一化掉长度
- 只看方向，不看大小 → 衡量语义相似度

---

## 1.4 矩阵基础辨析

### Q：矩阵1和矩阵2数字一样，但行顺序不同，是同一个矩阵吗？
**A：不是。**

矩阵的每一行对应一个 token 的向量。行顺序变了，意味着 token 和向量的对应关系变了，所以是不同的矩阵。

```
矩阵1（猫,狗,鱼）:        矩阵2（狗,猫,鱼）:
┌─────┬─────┐             ┌─────┬─────┐
│ 0.9 │ 0.1 │ ← 猫         │ 0.8 │ 0.2 │ ← 狗
│ 0.8 │ 0.2 │ ← 狗         │ 0.9 │ 0.1 │ ← 猫
│ 0.1 │ 0.9 │ ← 鱼         │ 0.1 │ 0.9 │ ← 鱼
└─────┴─────┘             └─────┴─────┘
   不同！                    不同！
```

---

# 第2课：位置编码

## 2.1 反直觉的事实：注意力是无序的

### 为什么 Transformer 没有位置感知？
Attention 的核心是点积：`Q · K^T`，点积满足交换律 `a·b = b·a`。

**推论：**
- 打乱 token 顺序，Attention 的输出（在重排后）完全相同
- "我 爱 你" 和 "你 爱 我" 在 Attention 看来是"同一个集合"

### 用置换矩阵证明
设 P 是置换矩阵（每行每列恰好一个1，其余0）。

```
原序列:      X = [x1, x2, x3]
打乱序列:    X' = P · X  （P 重排了行）

Attention 计算:
  Q = X · WQ,  K = X · WK
  Q' = P·Q,   K' = P·K
  
  Q' · K'^T = P·Q · (P·K)^T = P·Q·K^T·P^T
  
  softmax 后 V 的输出: P · (原输出)
```

**结论：** 打乱输入 = 打乱输出，Attention 本身感知不到顺序。FFN 对每个 token 独立处理，也不混合 token 间信息，同样无序。

---

## 2.2 位置编码公式

### 解决方案：给每个位置加一个唯一的位置向量

```
PE(pos, 2i)   = sin(pos / 10000^(2i/d_model))
PE(pos, 2i+1) = cos(pos / 10000^(2i/d_model))
```

| 符号 | 含义 | 取值 |
|------|------|------|
| pos | token 在序列中的位置 | 0, 1, 2, ... |
| i | 维度索引 | 0, 1, ..., d_model/2 - 1 |
| d_model | 模型维度 | 512 |

### 直觉理解
- 不同维度用不同频率的 sin/cos
- 低频维度（i 小）→ 捕捉长距离位置关系
- 高频维度（i 大）→ 捕捉短距离位置关系
- 每个位置的编码唯一

### 最终输入
```
输入向量 = Embedding(token) + PositionalEncoding(pos)
```

---

## 2.3 为什么用 sin/cos

| 方案 | 优点 | 缺点 |
|------|------|------|
| 学习式位置编码 | 灵活 | 不能外推到训练时没见过的长度 |
| **sin/cos 编码** | **能外推、能表达相对位置** | 固定形式 |

### 相对位置的表达
利用三角恒等式：
```
sin(pos + k) = sin(pos)cos(k) + cos(pos)sin(k)
cos(pos + k) = cos(pos)cos(k) - sin(pos)sin(k)
```
- 位置 `pos+k` 的编码可以表示为位置 `pos` 编码的线性变换
- 模型能学到"相对偏移"的关系

---

# 第3课：注意力机制

## 3.1 Q/K/V 的来源

### 三个投影矩阵
输入 x 经过三个不同的线性变换，得到 Q/K/V：

```
Q = x · WQ    （Query：我要找什么）
K = x · WK    （Key：我有什么可被找）
V = x · WV    （Value：我的实际内容）

WQ, WK 形状 [d_model, d_k]
WV 形状 [d_model, d_v]
```

### 直觉类比（图书馆检索）
- **Q（查询）**：你输入的搜索关键词
- **K（键）**：每本书的标题/标签
- **V（值）**：书的实际内容
- Q 和 K 算相似度 → 决定注意力权重 → 加权聚合 V

### 初始化与学习
- WQ/WK/WV 初始化为小的随机数（正态分布）
- 训练时通过反向传播更新，逐渐学到"该关注什么"

---

## 3.2 Attention 公式

```
Attention(Q, K, V) = softmax(Q · K^T / √d_k) · V
```

### 分步解析
```
1. Q · K^T         → 算每个 Q 和所有 K 的相似度（点积）
2. / √d_k          → 缩放（防止数值过大）
3. softmax         → 转成概率（和为1）
4. · V             → 用概率加权聚合 V
```

---

## 3.3 为什么除以 √d_k（核心难点）

### 问题：不缩放会怎样？
当 d_k 较大时，Q·K^T 的数值会很大 → softmax 进入饱和区 → 梯度消失。

### 推导：点积的方差

**前提：** Q 和 K 的每个分量独立，服从标准正态分布 N(0, 1)（均值0，方差1）。

**单分量乘积的方差：**
```
Var(q₁ · k₁) = E[(q₁·k₁)²] - (E[q₁·k₁])²

  E[q₁·k₁] = E[q₁]·E[k₁] = 0·0 = 0    （独立 → 期望可拆）

  E[(q₁·k₁)²] = E[q₁²] · E[k₁²]      （独立 → 平方期望可拆）
               = 1 · 1 = 1              （Var=E[X²]-(E[X])²，E[X]=0 → E[X²]=Var=1）

所以 Var(q₁·k₁) = 1 - 0 = 1
```

**d_k 个分量之和的方差：**
```
点积 S = q·k = q₁k₁ + q₂k₂ + ... + q_d k_d

独立随机变量和的方差 = 各方差之和
Var(S) = d_k × Var(q₁k₁) = d_k × 1 = d_k
```

### 缩放的作用
```
S / √d_k 的方差 = Var(S) / d_k = d_k / d_k = 1   ✓
```

**方差回到 1** → softmax 输入在合理范围 → 梯度正常流动。

---

## 3.4 softmax 函数

### 公式
```
softmax(si) = exp(si) / Σ exp(sj)
```

### 作用
- 把任意实数分数转成概率（全部 > 0，和 = 1）
- 指数放大差异：大的更大，小的更小

### 数值示例
```
分数:     [2.0,  1.0,  0.1]
exp:      [7.39, 2.72, 1.11]    ← 和 = 11.22
softmax:  [0.66, 0.24, 0.10]    ← 和 = 1.0
```

### 为什么和方差有关
- 方差大 → 分数差异大 → exp 后差异爆炸 → 概率接近 [1,0,0,...] → 梯度趋于 0（饱和）
- 方差小 → 分数接近 → 概率均匀 → 模型无法区分

**√d_k 缩放让方差 = 1，刚好在"可区分但不饱和"的甜区。**

---

## 3.5 数学基础专题

### 期望 E[X]
- 描述随机变量的"平均值"
- 离散：E[X] = Σ x · P(x)
- 连续：E[X] = ∫ x · f(x) dx

### 方差 Var(X)
```
Var(X) = E[(X - μ)²] = E[X²] - (E[X])²
```
- 描述数据离散程度
- 标准差 σ = √Var(X)

### 正态分布 N(μ, σ²)
```
f(x) = (1 / (σ√(2π))) · exp(-(x-μ)² / (2σ²))
```

**3σ 法则：**
- μ ± σ：68% 数据
- μ ± 2σ：95% 数据
- μ ± 3σ：99.7% 数据

### 高斯积分推导（为什么归一化常数是 1/√(2π)）

**目标：** 求 I = ∫ e^(-x²) dx

**技巧：** 算 I²（用二重积分）
```
I² = ∫∫ e^(-(x²+y²)) dx dy
```

**极坐标变换：**
- x² + y² = r²
- dxdy = r dr dθ（多出来的 r 是雅可比行列式）

```
I² = ∫(0到2π) dθ · ∫(0到∞) e^(-r²) · r dr

令 u = r², du = 2r dr → r dr = du/2

= 2π · (1/2) · ∫(0到∞) e^(-u) du
= π · 1
= π

所以 I = √π
```

**归一化：** 让 ∫ f(x) dx = 1，需要除以 σ√(2π)。

---

# 第4课：注意力变体

## 4.1 自注意力（Self-Attention）

### 特点
- Q、K、V 都来自**同一个序列**
- 让句子内部 token 互相看

### 作用
理解上下文。例如 "苹果" 在 "我吃苹果" 和 "苹果发布新手机" 中含义不同，自注意力让 "苹果" 看到周围词，调整自己的表示。

```
输入: "我 吃 苹果"
Q,K,V 都来自这三个 token
"苹果" 的输出 = 加权(我, 吃, 苹果) 的 V
```

---

## 4.2 交叉注意力（Cross-Attention）

### 特点
- Q 来自一个序列（Decoder）
- K、V 来自另一个序列（Encoder）

### 作用
让 Decoder 从 Encoder 提取信息，实现"翻译对齐"。

```
源句(Encoder输出): "I love you" → enc_out
目标句(Decoder):   "我 爱 ?"

Decoder 的 Q  ← 来自 "我 爱"
Encoder 的 K,V ← 来自 "I love you"

Cross-Attention: Q · K^T 算 "我 爱" 和 "I love you" 的对齐
              → 加权 V 得到翻译信息 → 预测下一个词 "你"
```

---

## 4.3 多头注意力（Multi-Head Attention）

### 动机
一个头只能学一种关系。多个头并行，各学各的。

### 实现
1. 把 d_model 切成 h 份，每份 d_k = d_model / h
2. h 个头各自做 Attention
3. concat 所有头的输出
4. 用 WO 投影回 d_model

```
d_model = 512, h = 8 → d_k = 64

头1: Q1,K1,V1 (64维) → Attention → out1 (64维)
头2: Q2,K2,V2 (64维) → Attention → out2 (64维)
...
头8: Q8,K8,V8 (64维) → Attention → out8 (64维)

concat[out1,...,out8] → [512] → × WO[512,512] → 最终输出 [512]
```

### 参数量
- WQ, WK, WV 各：d_model × d_model = 512×512
- WO：d_model × d_model = 512×512
- 总计：4 × d_model²

---

## 4.4 Masked 注意力

### 解决的问题
训练时已知完整目标句，如果不限制，模型会"偷看"未来词。但推理时未来词还没生成，看不到 → 训练和推理不一致。

### 解决方案：下三角掩码
```
Attention 分数矩阵（目标句 "我 爱 你"）:
        我   爱   你
我  [  0.6, -∞,  -∞ ]
爱  [  0.3, 0.5, -∞ ]
你  [  0.2, 0.3, 0.4 ]

softmax 后 -∞ → 0:
        我   爱   你
我  [  1.0,  0,   0  ]
爱  [  0.4, 0.6,  0  ]
你  [  0.2, 0.3, 0.5 ]
```

### Mask 的双重作用
1. **保证因果性**（本质）：生成时不能看未来
2. **并行训练**（附带好处）：训练时一次性输入完整目标句，用 Mask 模拟逐字生成

### 与 Cross-Attention 配合
- **Masked Self-Attention**：目标句内部对齐（模拟逐字生成）
- **Cross-Attention**：从源句提取翻译信息
- 两者配合完成翻译任务

---

## 4.5 FFN（前馈神经网络）

### 公式
```
FFN(x) = max(0, x·W1 + b1) · W2 + b2
```

### 结构
```
x [512] → W1[512, 2048] → [2048] → ReLU → W2[2048, 512] → [512]
         升维              非线性     降维
```

### 三个作用
1. **引入非线性**（ReLU）：Attention 是线性的，没有 ReLU 模型就是堆叠线性变换
2. **特征变换**：升维 → 降维，让模型有更大容量
3. **知识存储**：W1 的每一列是"记忆 Key"，W2 的对应行是"记忆 Value"，构成训练学到的知识库

### FFN 不混合 token 间信息
- FFN 对每个 token **独立**处理
- 混合 token 间信息的是 Attention
- 所以 FFN 不影响"顺序无序性"问题

---

# 第5课：完整 Transformer Block

## 5.1 残差连接

### 公式
```
y = Sublayer(x) + x
```

### 三个作用

#### 1. 解决梯度消失
```
y = f(x) + x
∂y/∂x = ∂f/∂x + 1     ← 这个 +1 保证梯度至少为1
```
反向传播时，梯度有"高速公路"直通底层，不会消失。

#### 2. 保留原始信息
+x 让原始 token 身份信息直通到深层，不被洗掉。

#### 3. 学增量而非全量
每层只需学"在输入基础上改多少"（增量），比学"从零重建"容易。

### 关键理解：每个子层各自有残差
```
Attention 的残差 = Attention 输出 + Attention 输入
FFN 的残差       = FFN 输出 + FFN 输入
```
**不跨层累加。**

### 放大倍数分析

**无残差：**
```
单层: y = W·x
n 层: y = W^n · x
W 微调 ΔW → 输出变化 ≈ n·W^(n-1)·ΔW·x

若 W=2, n=6: 放大 = 2^5 = 32 倍
W 变 0.01 → 输出跳变 0.32 → 损失剧烈震荡
```

**有残差：**
```
单层: y = (W+1)·x
n 层: y = (W+1)^n · x

训练后 W 收敛到小值（如 0.1）
n 层放大 = (1.1)^6 ≈ 1.77 倍（温和）
```

**残差的真正价值**：让 W 收敛到 <1 的"甜区"，避免放大爆炸。

---

## 5.2 LayerNorm

### 公式
```
μ = mean(x)                          ← 均值
σ² = var(x)                          ← 方差
x̂ = (x - μ) / √(σ² + ε)            ← 标准化（ε 防止除0）
output = γ ⊙ x̂ + β                  ← 仿射变换
```

### 作用
- 稳定每层输入的数值分布
- 防止训练时数值爆炸/消失

### 仿射变换（γ, β）
- 标准化后变成 N(0, 1)，但不是所有情况都适合
- γ 控制每个维度的方差（缩放）
- β 控制每个维度的均值（偏移）
- 让模型自己学最优分布

```
归一化后: N(0, 1)
γ⊙x̂ + β 后: N(β, γ²)
```

### 广播机制
- γ, β 形状 `[d]`（如 [512]）
- 输入形状 `[batch, seq_len, d]`（如 [3, 10, 512]）
- γ, β 自动复制到 `[3, 10, 512]`，逐元素运算

### ⊙ vs ·
| 符号 | 名称 | 运算 | 结果 |
|------|------|------|------|
| ⊙ | 逐元素乘（Hadamard） | 对应位置相乘 | 向量 |
| · | 点积 | 相乘再求和 | 标量 |

---

## 5.3 反向传播基础

### 链式法则
```
y = f(g(x))
∂y/∂x = ∂y/∂g · ∂g/∂x
```

### 几何意义
- 导数 = 斜率 = 变化率 = 放大倍数
- 告诉你"输入变 1，输出变多少"
- 梯度下降：朝斜率反方向走，找最低点

### LayerNorm 的梯度
```
∂L/∂γ = ∂L/∂y ⊙ x̂
∂L/∂β = ∂L/∂y
```
然后用梯度下降更新 γ, β。

---

## 5.4 Encoder vs Decoder Block

### Encoder Block（2 个子层）
```
y1 = LayerNorm1(x + SelfAttention(x))
output = LayerNorm2(y1 + FFN(y1))
```
- 任务：理解源句
- 用 Self-Attention，不用 Mask

### Decoder Block（3 个子层）
```
y1 = LayerNorm1(x + MaskedSelfAttention(x))
y2 = LayerNorm2(y1 + CrossAttention(Q=y1, K=enc_out, V=enc_out))
output = LayerNorm3(y2 + FFN(y2))
```
- 任务：生成目标句
- Masked Self-Attention：目标句内部对齐（保证因果性）
- Cross-Attention：从 Encoder 提取翻译信息
- FFN：特征变换 + 知识提取

### 翻译是怎么发生的？
**不是逐字翻译，也不是分段拼接。**
1. Encoder 把整句源句理解成语义向量 `enc_out`
2. Decoder 每生成一个词，用 `enc_out`（通过 Cross-Attention）+ 已生成的目标词，预测下一个词
3. 每个词的生成都用到完整源句信息

---

## 5.5 训练 vs 推理

### 训练（并行）
```
源句: "I love you" → Encoder → enc_out
目标句（已知答案）: "<start> 我 爱 你 <end>"

一次输入完整目标句，用 Mask 模拟逐字生成:
  位置1: 看到 <start>           → 预测 "我"
  位置2: 看到 <start> 我        → 预测 "爱"
  位置3: 看到 <start> 我 爱     → 预测 "你"
  位置4: 看到 <start> 我 爱 你  → 预测 <end>

所有位置并行计算，一次 forward 得到所有预测
```

### 推理（串行）
```
第1步: 输入 <start> → 预测 "我"
第2步: 输入 <start> 我 → 预测 "爱"
第3步: 输入 <start> 我 爱 → 预测 "你"
第4步: 输入 <start> 我 爱 你 → 预测 <end>（停止）

逐字生成，每步重新跑 Decoder
（现代 LLM 用 KV Cache 加速）
```

### 损失函数（在哪一步？）
在 softmax 之后，用**交叉熵损失**：
```
Loss = -log(模型给真实词的概率)
```
- 预测越准 → 概率越接近 1 → Loss 越小
- 训练时每个位置都算 Loss，最后取平均

---

## 5.6 完整数据流

```
文本
 ↓ Tokenization
token IDs
 ↓ Embedding 查表
向量序列 [seq_len, d_model]
 ↓ + 位置编码
带位置信息的向量
 ↓ Transformer Block × N
    ├─ Multi-Head Attention + 残差 + LayerNorm
    └─ FFN + 残差 + LayerNorm
深层语义表示
 ↓ 输出层（h · E^T）
V 个分数
 ↓ softmax
V 个概率
 ↓ argmax
预测的 token ID
 ↓ 查词表
预测的词
```

---

# 第6课：从原论文到现代 LLM 的演进

## 6.1 Pre-Norm vs Post-Norm

| | Post-Norm（原版） | Pre-Norm（现代） |
|---|---|---|
| 公式 | `LN(x + Sub(x))` | `x + Sub(LN(x))` |
| 主通路 | 经过 LN | +x 直通 |
| 深层稳定性 | 差 | 好 |

**现代 LLM 选 Pre-Norm**：主通路不经过 LayerNorm，梯度有"高速公路"直通，深层训练稳定。

---

## 6.2 三大架构路线

| 架构 | 代表模型 | 适合任务 | 特点 |
|------|---------|---------|------|
| Encoder-Only | BERT | 理解（分类、NER） | 双向注意力 |
| Decoder-Only | GPT, LLaMA | 生成 | 单向注意力 |
| Encoder-Decoder | T5 | 翻译、摘要 | 完整 Transformer |

**现代 LLM 选 Decoder-Only**：
- 生成任务天然单向
- Scaling law 表现好
- 训练数据利用效率高（无监督下一词预测）

---

## 6.3 现代 LLM 的关键改进

| 组件 | 原版 | 现代 LLM | 改进点 |
|------|------|---------|--------|
| Norm 位置 | Post-Norm | Pre-Norm | 深层稳定 |
| Norm 类型 | LayerNorm | RMSNorm | 速度快 |
| 位置编码 | sin/cos | RoPE | 长序列外推 |
| 激活函数 | ReLU | SwiGLU | 效果更好 |
| Attention | MHA | GQA | 推理省内存 |

### RMSNorm（简化版 LayerNorm）
- 去掉"减均值"步骤，只算均方根
- 速度快 10-50%
- 效果接近 LayerNorm

```
RMSNorm(x) = γ ⊙ x / √(mean(x²) + ε)
```

### RoPE（旋转位置编码）
- 不把位置编码加到输入
- 而是在 Attention 的 Q·K^T 里注入位置信息（旋转矩阵）
- 长序列外推能力好

### SwiGLU（门控激活）
- 双分支：主分支 + 门控分支
- 门控决定主分支信息通过多少
- 多用 1/3 参数换更好效果

```
SwiGLU(x) = (x·W1 · Swish(x·W2)) · W3
```

### GQA（分组查询注意力）
- 多个 Q 头共享一组 K/V
- 减少 KV Cache 内存
- 推理更快，效果接近 MHA

---

## 6.4 现代 Decoder-Only Block

```
y1 = x + Attention(RMSNorm(x))          ← Pre-RMSNorm + RoPE
output = y1 + SwiGLU(RMSNorm(y1))       ← SwiGLU FFN
```

对比原版 Decoder Block：
- Pre-Norm 替代 Post-Norm
- RMSNorm 替代 LayerNorm
- RoPE 替代 sin/cos
- SwiGLU 替代 ReLU FFN
- 去掉 Cross-Attention（Decoder-Only 不需要）

---

# 附录：数学基础专题

## A.1 期望与方差

### 期望 E[X]
- **离散**：E[X] = Σ x · P(x)
- **连续**：E[X] = ∫ x · f(x) dx
- 描述随机变量的"平均值"

**示例（掷骰子）：**
```
X = {1,2,3,4,5,6}, 每个概率 1/6
E[X] = (1+2+3+4+5+6)/6 = 3.5
```

### 方差 Var(X)
```
Var(X) = E[(X - μ)²] = E[X²] - (E[X])²
```
- 描述数据离散程度
- 标准差 σ = √Var(X)

**推导：**
```
Var(X) = E[(X-μ)²]
       = E[X² - 2Xμ + μ²]
       = E[X²] - 2μ·E[X] + μ²
       = E[X²] - 2μ² + μ²        （E[X] = μ）
       = E[X²] - μ²
```

### 独立随机变量的性质
- E[XY] = E[X]·E[Y]
- Var(X+Y) = Var(X) + Var(Y)
- Var(c·X) = c²·Var(X)
- Var(X+c) = Var(X)

---

## A.2 正态分布

### 概率密度函数
```
f(x) = (1 / (σ√(2π))) · exp(-(x-μ)² / (2σ²))
```

### 概率密度是什么？
- 描述连续型随机变量在某点附近的"概率密集程度"
- 概率 = 概率密度 × 区间长度
- 类比：速度（密度）× 时间（区间）= 距离（概率）

### 3σ 法则
| 范围 | 概率 |
|------|------|
| μ ± σ | 68% |
| μ ± 2σ | 95% |
| μ ± 3σ | 99.7% |

### xy 轴含义
- x 轴：随机变量的取值
- y 轴：概率密度（不是概率本身）
- 曲线下总面积 = 1

---

## A.3 高斯积分推导

### 目标
求 I = ∫(−∞到∞) e^(-x²) dx

### 技巧：算 I²
```
I² = I × I
   = [∫ e^(-x²) dx] × [∫ e^(-y²) dy]
   = ∫∫ e^(-x²) · e^(-y²) dx dy
   = ∫∫ e^(-(x²+y²)) dx dy      ← 指数相乘 = 指数相加
```

### 极坐标变换
```
x² + y² = r²
dxdy = r dr dθ    （雅可比行列式，多出的 r 不能忽略）

I² = ∫(0到2π) dθ · ∫(0到∞) e^(-r²) · r dr
   = 2π · ∫(0到∞) r·e^(-r²) dr

令 u = r², du = 2r dr → r dr = du/2
   = 2π · (1/2) · ∫(0到∞) e^(-u) du
   = π · [-e^(-u)](0到∞)
   = π · (0 - (-1))
   = π

所以 I = √π
```

### 归一化常数
标准正态分布 f(x) = A·e^(-x²/2) 的积分要等于 1：
```
∫ A·e^(-x²/2) dx = 1
A · √(2π) = 1     （用类似方法推导）
A = 1/√(2π)
```

---

## A.4 数学知识在各课的应用

| 数学概念 | 在哪用 | 作用 |
|---------|--------|------|
| 期望 E[X] | 第3课 | 分析点积分布 |
| 方差 Var(X) | 第3课 | 推导 √d_k 缩放 |
| 正态分布 | 第3课 | Q/K 初始化 |
| 高斯积分 | 第3课 | 推导归一化常数 |
| 极坐标变换 | 第3课 | 求解高斯积分 |
| 点积 | 第3课 | 计算相似度 |
| softmax | 第3课 | 分数转概率 |
| 3σ 法则 | 第3课 | 理解数据分布范围 |
| 向量加法 | 第5课 | 残差相加 |
| 均值/方差 | 第5课 | LayerNorm 统计量 |
| Z-score 标准化 | 第5课 | LayerNorm 核心变换 |
| 仿射变换 y=γx+β | 第5课 | LayerNorm 的 γ/β |
| 链式法则 | 第5课 | 反向传播 |
| 导数 = 变化率 | 第5课 | 理解放大倍数 |
| 广播机制 | 第5课 | γ/β 与批量运算 |
| 逐元素乘 ⊙ | 第5课 | LayerNorm 运算 |
| 交叉熵 | 第5课 | 训练损失函数 |
| 置换矩阵 | 第2课 | 证明注意力无序 |

---

# 核心概念速查表

| 概念 | 一句话解释 |
|------|-----------|
| Tokenization | 把文本切成子词单元 |
| Embedding | 用 ID 查表得到向量 |
| 权重共享 | Embedding 和输出层用同一个矩阵 |
| 位置编码 | 给 token 加上位置信息 |
| Q/K/V | 查询/键/值，注意力的核心 |
| Attention | token 间信息混合 |
| √d_k 缩放 | 防止 softmax 饱和 |
| softmax | 分数转概率 |
| 自注意力 | 句子内部 token 互相看 |
| 交叉注意力 | Decoder 从 Encoder 提取信息 |
| 多头注意力 | 多视角并行关注 |
| Mask | 防止看未来，保证因果性 |
| FFN | 引入非线性，存储知识 |
| 残差连接 | y=f(x)+x，学增量保信息 |
| LayerNorm | 稳定数值，γ/β 调分布 |
| 交叉熵损失 | -log(预测真实词的概率) |
| Pre-Norm | 先归一化再做子层变换 |
| RMSNorm | 简化版 LayerNorm |
| RoPE | 旋转位置编码 |
| SwiGLU | 门控激活函数 |
| GQA | 分组共享 K/V 的注意力 |

---

# 第7课：数据集与训练流程

## 7.1 训练的三个阶段

Transformer / LLM 的训练通常分三个阶段，每阶段数据格式完全不同：

| 阶段 | 名称 | 数据格式 | 规模 | 目标 |
|------|------|---------|------|------|
| 1 | 预训练 Pre-training | 海量纯文本（无标注） | 万亿 token | 学语言规律 |
| 2 | 监督微调 SFT | 指令-回答对 | 几万~几十万条 | 学会听指令 |
| 3 | 对齐训练 RLHF/DPO | 人类偏好对比对 | 几万~几十万条 | 学人类喜好 |

---

## 7.2 阶段1：预训练数据

### 数据格式：就是普通文本
```
文件1.txt:
  "人工智能是计算机科学的一个分支，它企图了解智能的实质..."
  "今天天气不错，适合出去散步。我喜欢在公园里看书..."

文件2.txt:
  "def quicksort(arr):
       if len(arr) <= 1:
           return arr
       ..."
```

### 数据规模参考
| 模型 | 预训练数据量 |
|------|------------|
| GPT-2 | 40GB 文本 |
| LLaMA-2 | 2万亿 token |
| GPT-4 | 估计 13万亿 token |

### 数据来源
- 网页爬取（Common Crawl）
- 书籍（BookCorpus）
- 代码（GitHub）
- 维基百科等

**关键：没有"问题-答案"对，让模型学"给定前文，预测下一个词"。**

---

## 7.3 阶段2：监督微调（SFT）数据

### 数据格式：问答对
```json
{
  "instruction": "把下面的句子翻译成英文",
  "input": "我爱学习人工智能",
  "output": "I love studying artificial intelligence"
}
```

```json
{
  "instruction": "解释什么是注意力机制",
  "input": "",
  "output": "注意力机制是一种让模型关注输入中重要部分的方法..."
}
```

---

## 7.4 阶段3：对齐训练数据

### 数据格式：同一个问题，两个回答，标注哪个更好
```json
{
  "prompt": "如何写一个冒泡排序？",
  "chosen": "冒泡排序的基本思路是...",      ← 人类觉得好
  "rejected": "我不知道，你自己去查吧。"    ← 人类觉得差
}
```

---

## 7.5 原论文翻译任务用的是平行语料

原版 Transformer 论文做的是翻译，用的是**平行语料**：

```
英文: "I love you"          → 中文: "我爱你"
英文: "The cat sits on mat" → 中文: "猫坐在垫子上"
英文: "Good morning"        → 中文: "早上好"
...（几百万句对）
```

经典数据集 **WMT14 英德**有 450 万句对。

---

## 7.6 完整训练流程（以预训练为例）

### 步骤1：数据预处理

```
原始文本
  ↓ 分词（Tokenizer）
token IDs: [4521, 893, 22, 7781, 902, 334, 11, 998, ...]
  ↓ 切成固定长度（如 1024）
块1: [4521, 893, 22, 7781, ..., 902]      ← 1024个
块2: [3344, 11, 998, ..., 7700]            ← 1024个
  ↓ 打包成 batch
batch: [块1, 块2, ..., 块32]               ← 32个块一起训
```

### 步骤2：构造训练样本（自监督）

**核心技巧：同一段文本，左边当输入，右边当答案。**

```
文本块: [4521, 893, 22, 7781, 902, 334, 11, 998]

输入 X = [4521, 893, 22, 7781, 902, 334, 11]      ← 前7个token
答案 Y = [893,  22, 7781, 902, 334, 11, 998]      ← 后移一位

模型每个位置都要预测下一个词:
  位置1: 看到 4521        → 预测 893
  位置2: 看到 4521,893    → 预测 22
  位置3: 看到 4521,...,22 → 预测 7781
  ...
```

**不需要人工标注！答案就在数据本身里。这就是"自监督"。**

### 步骤3：一次训练步骤（Step）

```
步骤1: 取一个 batch（32个文本块）
       X: [32, 1024]   ← 32个块，每块1024个token

步骤2: 前向传播（Forward）
       X → Embedding → Transformer × N层 → 输出 logits
       logits: [32, 1024, V]   ← V是词表大小(如5万)
       每个位置预测5万个词的分数

步骤3: 算损失（交叉熵）
       真实答案 Y: [32, 1024]
       Loss = -平均(log P(正确词))
       
       例: 位置1真实词是893
           模型给893的概率是0.3
           Loss = -log(0.3) = 1.2

步骤4: 反向传播（Backward）
       算 ∂Loss/∂每个参数
       （用链式法则，见第5课5.3节）

步骤5: 更新参数
       θ = θ - lr × ∂Loss/∂θ
       lr是学习率（如 0.0001）
```

### 步骤4：训练循环伪代码

```python
for epoch in range(num_epochs):          # 跑几轮
    for batch in dataloader:             # 每轮遍历所有数据
        X, Y = batch
        logits = model(X)                # 前向
        loss = cross_entropy(logits, Y)  # 算损失
        loss.backward()                  # 反向
        optimizer.step()                 # 更新参数
        optimizer.zero_grad()            # 清空梯度
```

---

## 7.7 训练关键技巧

| 技巧 | 作用 | 通俗解释 |
|------|------|---------|
| **学习率预热** | 防止初期震荡 | 先小步走，再大步走，再小步走 |
| **梯度裁剪** | 防止梯度爆炸 | 梯度太大就截断到固定值 |
| **Dropout** | 防止过拟合 | 随机"关掉"一部分神经元 |
| **混合精度** | 省显存、加速 | 用16位浮点数代替32位 |
| **数据并行** | 加速训练 | 多张GPU各算一个batch |

---

## 7.8 用前面学过的知识串起来

```
训练数据: "我爱学习AI"
  ↓ Tokenization（第1课）
[4521, 893, 22, 7781, 902]  ← 5个token
  ↓ Embedding查表 + 位置编码（第1、2课）
向量序列
  ↓ Transformer Block × N
    ├─ Masked Self-Attention（第4课，防止看未来）
    ├─ FFN（第4课）
    ├─ 残差 + LayerNorm（第5课）
    └─ (重复N层)
logits → softmax（第3课）→ 概率
  ↓ 交叉熵损失
Loss = -log(P(正确词))
  ↓ 反向传播（链式法则，第5课）
更新所有 WQ, WK, WV, W1, W2, γ, β, E...
```

**这就是前面所有组件如何协同工作来训练一个模型。**

---

## 7.9 三阶段对比总结

| | 预训练 | SFT | RLHF/DPO |
|---|---|---|---|
| **数据** | 海量纯文本 | 指令-回答对 | 偏好对比对 |
| **规模** | 万亿 token | 几万~几十万条 | 几万~几十万条 |
| **目标** | 学语言规律 | 学会听指令 | 学人类喜好 |
| **损失** | 下一词预测 | 下一词预测(只在回答部分) | 偏好排序 |
| **比喻** | 读完全世界书 | 学会回答问题 | 学会讨人喜欢 |

---

# 学习路径建议

## 已完成（本课程）
✅ Transformer 架构全貌
✅ 核心数学推导
✅ 现代 LLM 改进
✅ 数据集与训练流程

## 下一步可深入
1. **训练细节**：学习率调度、预热、梯度裁剪、Dropout
2. **特定 LLM 架构**：LLaMA / Qwen / GLM 的具体实现差异
3. **微调技术**：LoRA、QLoRA、PEFT
4. **对齐训练**：RLHF、DPO、GRPO
5. **推理优化**：KV Cache、量化、投机解码
6. **长上下文**：YaRN、滑动窗口注意力、稀疏注意力
7. **MoE 架构**：Mixtral、DeepSeek-V3 的专家路由

---

*本课程基于用户从零开始学习 Transformer 的真实问答记录整理，涵盖所有核心知识点、数学推导和常见疑问。*
